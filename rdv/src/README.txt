- This program is a GuI-based program.
- This program does not require any compilation.
- To run this program, run the following command, assuming that path to run_RDV is set up.

$ run_RDV [binpath] //[binpath] is the path where run_RDV is located.
                        //[binpath] can be skipped if run_RDV is launched in ../bin directory.

